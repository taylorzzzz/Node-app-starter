/*
    If we are using an online db host we 
    would put the url here.

    If we are hosting locally we would put the
    location here too.
*/

module.exports = {
    mongoURI: 'mongodb://<db-user>:<db-password>@ds231588.mlab.com:31588/example-dev'
}